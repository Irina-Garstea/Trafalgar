import styled from '@emotion/styled';

export const Button = styled.button`
  background-color: #ffffff;
  color: #4089ed;
  font-size: 16px;
  letter-spacing: 1px;
  font-weight: 500;
  border: none;
  cursor: pointer;

  span {
    padding: 20px;
  }
`;
